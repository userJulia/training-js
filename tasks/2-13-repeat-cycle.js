'use strict';

let number;

do {
  number = prompt('Enter a number greater then 100');
} while (number < 100 && number !== null);

/*
let userName;
let password;

do {
  userName = prompt('enter login');
} while (!userName);

do {
  password = prompt('enter password');
} while (!password);

if (userName === 'admin') {
  alert('admin');
} else {
  alert('user');
}
*/
