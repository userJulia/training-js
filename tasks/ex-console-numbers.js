'use strict';

function consoleNumbers(n) {
  for (let j = 0; j <= n; j++) {
    console.log(j);
  }
  for (let i = n; i >= 0; i--) {
    console.log(i);
  }
}

consoleNumbers(5);

//1. создать функцию, которая выводит в консоль числа от 0 до n и от n до 0
